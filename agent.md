# agent.md

이 문서는 어떤 코드 프로젝트에도 적용할 수 있는 Codex 공통 작업 규칙이다.
프로젝트별 도메인, 제품 요구사항, 기술 스택, 배포 환경은 이 문서에 직접 넣지 않고 별도 도메인 문서에 둔다.

## 적용 방식

공통 문서와 도메인 문서를 분리한다.

- 공통 규칙: `agent.md`, `prompt.md`, `prompts/*.md`
- 프로젝트 맥락: `project-context.md` 또는 `docs/project-context.md`
- 상세 설계: `docs/requirements.md`, `docs/architecture.md`, `docs/testing.md`, `docs/decisions/*.md`

작업 세션은 시작 시 아래 순서로 근거를 확인한다.

1. `agent.md`
2. 해당 세션 프롬프트
3. 프로젝트 도메인 문서
4. 관련 소스 코드, 테스트, 설정, 로그

도메인 문서가 없으면 임의로 가정하지 않고 `미결정` 또는 `확인 필요`로 표시한다.

## 작업 순서

모든 구현 작업은 아래 순서를 따른다.

1. 설계
2. 최소 구현
3. 테스트 추가
4. 리팩터링

순서를 건너뛰지 않는다. 긴급 버그 수정도 최소한의 설계와 재현 근거를 먼저 확보한다.

## 세션 진행 프로토콜

Codex는 코드 변경이 필요한 작업에서 아래 순서를 지킨다.

1. 관련 파일 찾기
2. 변경안 제시
3. 수정
4. 테스트/린트 실행
5. 결과 요약 + 다음 액션 제시

### 1. 관련 파일 찾기

- `rg`, `rg --files`, 테스트 파일, 로그, 설정 파일을 먼저 확인한다.
- 근거 없이 구조나 동작을 추측하지 않는다.
- 확인한 근거는 파일 경로, 테스트 이름, 명령 출력으로 인용한다.

### 2. 변경안 제시

수정 전 짧게 제시한다.

- 변경 범위
- 변경하지 않을 범위
- 예상 리스크
- 검증 방법

### 3. 수정

- 목표 달성에 필요한 최소 변경만 한다.
- 사용자 또는 다른 세션이 만든 변경을 되돌리지 않는다.
- 예상 밖 변경이 발견되면 즉시 멈추고 사용자에게 확인한다.
- 구현 중 새 가정이 필요하면 코드로 밀어붙이지 말고 근거를 먼저 찾는다.

### 4. 테스트/린트 실행

- 가능한 경우 관련 테스트와 린트를 실행한다.
- 실행하지 못한 경우 이유와 실행해야 할 정확한 명령을 남긴다.
- 실패하면 실패 로그의 핵심 줄을 인용하고, 수정 여부를 사용자에게 명확히 알린다.

### 5. 결과 요약 + 다음 액션

완료 보고에는 아래 항목을 포함한다.

- 변경 파일 목록
- 핵심 diff 요약
- 실행한 테스트/린트와 결과
- 리스크 또는 사이드이펙트
- 롤백 방법
- 다음 액션

## 코드 변경 보고 형식

코드 변경 후에는 다음 형식을 사용한다.

```md
변경 파일
- path/to/file

핵심 변경
- 무엇을 왜 바꿨는지 요약

검증
- 실행 명령: `...`
- 결과: 통과/실패/미실행

리스크
- 예상 가능한 사이드이펙트

롤백
- 커밋 전: `git restore <파일>`
- 커밋 후: `git revert <커밋>`

다음 액션
- 필요한 후속 작업
```

## 설계 기준

초기 설계는 아래 관심사를 분리한다.

- 목표: 사용자가 달성하려는 결과
- 범위: 이번 작업에 포함할 것과 제외할 것
- 도메인 규칙: 프로젝트별 정책, 용어, 제약
- 데이터 모델: 상태, 영속성, 권한, 동시성
- 인터페이스: UI, API, CLI, 이벤트, 외부 연동
- 오류 처리: 실패 조건, 복구 방식, 사용자 피드백
- 검증 전략: 단위 테스트, 통합 테스트, E2E, 수동 확인
- 운영 영향: 성능, 보안, 접근성, 배포, 롤백

도메인 규칙은 공통 문서에 직접 쓰지 않고 프로젝트 도메인 문서에 연결한다.

## 테스트 기준

최소 테스트 범위는 프로젝트 도메인에 맞게 정하되, 아래 기준을 우선 검토한다.

- 핵심 사용자 흐름
- 데이터 변환과 상태 전이
- 권한과 입력 검증
- 오류/빈 상태/경계값
- 외부 연동 실패
- 회귀 가능성이 높은 버그 재현 케이스

버그 수정 시:

- 재현 테스트를 먼저 추가하거나, 기존 테스트/로그로 재현 근거를 남긴다.
- 수정 후 같은 테스트가 통과해야 한다.

## 문서 우선 세션 규칙

문서 작성 중심 세션에서는 원칙적으로 Markdown 문서만 수정한다.

- 허용: `*.md`, 설계 문서, 세션 프롬프트, 작업 규칙
- 금지: 애플리케이션 코드 수정, 의존성 설치, 포맷터 설정 변경
- 예외: 사용자가 명시적으로 코드 수정을 요청한 경우

## 커뮤니케이션 규칙

- 한국어로 간결하게 보고한다.
- 변경 전에는 무엇을 바꿀지 짧게 말한다.
- 완료 후에는 근거, 결과, 리스크, 롤백 방법을 남긴다.
- 모르는 내용은 가정하지 않는다.
- 근거가 필요한 내용은 파일, 테스트, 로그, 공식 문서 링크로 확인한다.
