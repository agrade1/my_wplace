# project-context.md

이 문서는 현재 프로젝트의 도메인 맥락과 제품 요구사항을 정리한다.
공통 작업 규칙은 `agent.md`, 세션 운영 방식은 `prompt.md`와 `prompts/*.md`를 따른다.

## 프로젝트 개요

- 프로젝트 이름: `my_wplace`
- 한 줄 설명: 서울 지역 한정 wplace 스타일 픽셀 드로잉 지도 서비스
- 목표 사용자: 서울 지도 위에 픽셀 단위 그림을 그리고 싶은 사용자
- 해결하려는 문제: 넓은 지도 서비스 전체를 구현하지 않고, 서울 지역만 대상으로 지도 기반 픽셀 드로잉 경험을 축소 구현한다.

## 목표

- 서울 지역만 대상으로 하는 지도 화면을 제공한다.
- 일정 줌 레벨 미만에서는 지도만 보이게 한다.
- 일정 줌 레벨 이상에서는 지도 위에 픽셀화된 그림 또는 픽셀 그리드를 보이게 한다.
- 사용자가 픽셀을 선택하고 색상을 적용할 수 있게 한다.
- 한 번에 여러 픽셀을 선택하고 색칠할 수 있게 한다.
- 키보드와 마우스를 조합한 빠른 선택/선택 해제 흐름을 제공한다.

## 비목표

MVP 범위에서 하지 않을 일을 명시한다.

- 전 세계 지도 전체를 대상으로 하지 않는다.
- 서울 외 지역을 우선 구현하지 않는다.
- 실제 wplace 서비스의 모든 기능을 동일하게 복제하지 않는다.
- 결제, 계정, 권한, 랭킹, 소셜 기능은 현재 요구사항에 포함하지 않는다.
- 실시간 다중 사용자 동기화는 MVP 필수 범위에는 포함하지 않는다.

## 장기 목표

- 최종적으로 여러 사용자가 같은 지도 영역에서 동시에 상호작용할 수 있게 한다.
- 다른 사용자의 픽셀 변경이 현재 사용자 화면에 실시간으로 반영되게 한다.
- 같은 영역을 보고 있는 사용자들의 접속 상태 또는 커서/활동 상태를 표시하는 방안을 검토한다.
- 동시 색칠 충돌, 낙관적 업데이트, 서버 확정 상태 반영 정책을 설계한다.
- 실시간 기능은 MVP 이후 단계적으로 도입한다.

## 핵심 용어

| 용어 | 의미 |
| --- | --- |
| 지도 | 사용자가 확대/축소하고 이동할 수 있는 서울 지역 지도 화면 |
| 줌 레벨 | 지도의 확대 정도를 나타내는 값 |
| 픽셀 레이어 | 특정 줌 레벨 이상에서 지도 위에 표시되는 픽셀 그리드 또는 그림 레이어 |
| 픽셀 | 사용자가 선택하고 색상을 적용할 수 있는 최소 칠하기 단위 |
| 선택 영역 | 사용자가 현재 색상을 적용하려고 선택해 둔 하나 이상의 픽셀 |
| 색상 적용 | 선택된 픽셀에 사용자가 고른 색상을 반영하는 동작 |
| 실시간 상호작용 | 여러 사용자의 픽셀 변경, 접속 상태, 커서/활동 상태가 지연 없이 반영되는 경험 |

## 핵심 기능

- 서울 지역 지도 표시
- 줌 레벨에 따른 픽셀 레이어 표시/숨김
- 단일 픽셀 클릭 선택
- 여러 픽셀 선택
- 선택된 픽셀에 색상 적용
- `Space` 키를 누른 상태에서 마우스를 움직일 때 지나간 픽셀 다중 선택
- 우클릭 또는 우클릭 드래그로 선택된 픽셀 선택 해제

## 장기 기능 후보

- 픽셀 변경 실시간 반영
- 같은 지도 구역을 보고 있는 사용자 presence 표시
- 다른 사용자의 커서 또는 선택 영역 표시
- 동시 편집 충돌 처리
- 사용자별 색칠 기록 또는 활동 로그
- 부정 사용 방지를 위한 제한 정책

## 주요 사용자 흐름

1. 사용자가 서울 지역 지도를 연다.
2. 사용자가 지도를 확대한다.
3. 줌 레벨이 임계값 이상이 되면 픽셀 레이어가 보인다.
4. 사용자가 픽셀을 클릭해 선택한다.
5. 사용자가 색상을 고른다.
6. 사용자가 선택된 픽셀에 색상을 적용한다.
7. 사용자가 여러 픽셀을 선택해야 할 경우 `Space`를 누른 상태로 마우스를 움직인다.
8. 사용자가 선택을 취소해야 할 경우 우클릭하거나 우클릭을 유지한 채 선택 영역을 지나간다.

## 도메인 규칙

- 픽셀 레이어는 지정된 줌 임계값 이상에서만 표시한다.
- 줌 임계값 미만에서는 사용자가 그림보다 지도를 우선 인지할 수 있어야 한다.
- 클릭은 해당 위치의 픽셀을 선택한다.
- `Space`를 누른 상태에서 마우스가 지나간 픽셀은 모두 선택 대상이 된다.
- 우클릭은 해당 픽셀 또는 선택 영역을 선택 해제한다.
- 우클릭을 유지한 상태로 선택된 영역을 지나가면 지나간 픽셀을 선택 해제한다.
- 색상 변경은 선택된 픽셀에 적용된다.
- 한 번에 여러 픽셀을 색칠할 수 있어야 한다.

## 기술 제약

- 프레임워크: `Next.js App Router + React + TypeScript`
- 런타임: `Node.js 22.x` 우선
- 지도 라이브러리: `MapLibre GL JS` 또는 React 바인딩(`react-maplibre`) 우선 검토
- 데이터 저장소: `Supabase Postgres`
- 외부 연동: `Supabase Auth`, `Supabase Realtime`은 장기 목표에 맞춰 단계적으로 도입
- 배포 환경: `Vercel`

## 기술 선정 근거

- React + TypeScript는 기본 UI 구현과 지도/픽셀 상태 관리의 기준 스택으로 사용한다.
- Next.js는 Vercel 배포와 Supabase SSR/Auth 연동을 고려해 우선 선택한다.
- 지도와 픽셀 편집 화면은 브라우저 상호작용이 핵심이므로 Next.js 안에서도 client component 중심으로 구현한다.
- Supabase는 Postgres 기반 저장소, 인증, Realtime을 한 플랫폼에서 제공하므로 초기 저장 구조부터 장기 실시간 기능까지 같은 백엔드 계열로 확장할 수 있다.
- MapLibre GL JS는 오픈소스 TypeScript 지도 렌더링 라이브러리이며 WebGL 기반이라 픽셀/레이어 오버레이 확장에 적합하다.

## 기술 선택 보류 사항

- 지도 타일 제공자와 사용량 비용
- `MapLibre GL JS` 직접 사용 vs `react-maplibre` 사용
- Supabase Realtime을 어느 단계부터 사용할지
- 인증을 MVP부터 넣을지, 익명 사용자 기반으로 시작할지
- 실시간 presence와 cursor를 구현할지, 픽셀 변경 동기화만 먼저 구현할지

## 테스트 우선순위

- 줌 임계값 미만에서 픽셀 레이어가 보이지 않는지
- 줌 임계값 이상에서 픽셀 레이어가 보이는지
- 클릭한 픽셀이 선택되는지
- 여러 픽셀이 동시에 선택되고 색칠되는지
- `Space` 드래그로 지나간 픽셀이 모두 선택되는지
- 우클릭으로 선택된 픽셀이 해제되는지
- 우클릭 드래그로 지나간 선택 픽셀이 해제되는지
- 색상 적용이 선택된 픽셀에만 반영되는지
- 장기 실시간 기능 도입 시, 원격 픽셀 변경이 현재 화면에 반영되는지
- 장기 실시간 기능 도입 시, 동시 수정 충돌 정책이 일관되게 동작하는지

## 미결정 사항

- 서울 지역 bounds 정의 방식
- 픽셀 셀 크기와 좌표 변환 방식
- 픽셀 데이터 저장 방식
- 줌 임계값
- 색상 팔레트 범위
- 모바일 터치 입력 지원 여부
- Supabase Realtime 도입 시점
- 지도 타일 제공자와 비용 정책
- 인증 도입 시점
- 실시간 상호작용 범위
- 동시 편집 충돌 해결 정책
- presence/cursor 표시 여부

## 참고 자료

- 사용자 초기 요구사항: wplace 스타일 서비스를 서울 지역 한정 축소 버전으로 구현
- 공통 작업 규칙: `agent.md`
- 세션 운영 프롬프트: `prompt.md`, `prompts/*.md`
